name: Thêm bài viết từ URL

on:
  workflow_dispatch:
    inputs:
      url:
        description: 'URL bài viết gốc'
        required: true
      source_label:
        description: 'Tên nguồn (vd: ByteByteGo)'
        required: false
        default: 'ByteByteGo'
      category:
        description: 'Category'
        required: true
        default: 'AI Notes'
      category_order:
        description: 'Thứ tự category'
        required: false
        default: '3'
      output_folder:
        description: 'Thư mục lưu bài (tên folder, vd: ai-notes)'
        required: false
        default: 'ai-notes'
      translate:
        description: 'Dịch sang tiếng Việt'
        type: boolean
        required: false
        default: false

permissions:
  contents: write
  actions: write

jobs:
  add-article:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Cài dependencies
        run: |
          pip install deep-translator

      - name: Fetch bài viết qua r.jina.ai
        run: |
          echo "Fetching: ${{ inputs.url }}"
          curl -s \
            -H "Accept: text/markdown" \
            -H "X-Return-Format: markdown" \
            "https://r.jina.ai/${{ inputs.url }}" \
            -o /tmp/article_raw.md

          CHARS=$(wc -c < /tmp/article_raw.md)
          echo "Fetched $CHARS bytes"
          if [ "$CHARS" -lt 200 ]; then
            echo "ERROR: Nội dung quá ngắn, có thể URL bị chặn"
            exit 1
          fi

      - name: Tạo file .md đúng chuẩn
        run: |
          TRANSLATE_FLAG=""
          if [ "${{ inputs.translate }}" = "true" ]; then
            TRANSLATE_FLAG="--translate"
            echo "🇻🇳 Sẽ dịch sang tiếng Việt"
          fi

          python .github/scripts/create_article.py \
            --content-file /tmp/article_raw.md \
            --url "${{ inputs.url }}" \
            --source-label "${{ inputs.source_label }}" \
            --category "${{ inputs.category }}" \
            --category-order ${{ inputs.category_order }} \
            --output-dir "src/content/articles/${{ inputs.output_folder }}" \
            $TRANSLATE_FLAG

      - name: Commit và push
        id: commit
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add src/content/articles/
          if git diff --staged --quiet; then
            echo "Không có file mới"
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            MSG="feat: thêm bài từ ${{ inputs.url }}"
            if [ "${{ inputs.translate }}" = "true" ]; then
              MSG="feat: thêm bài (VI) từ ${{ inputs.url }}"
            fi
            git commit -m "$MSG"
            git push
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Trigger deploy
        if: steps.commit.outputs.changed == 'true'
        run: |
          curl -s -X POST \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${{ github.repository }}/actions/workflows/deploy.yml/dispatches" \
            -d '{"ref":"main"}'
          echo "✅ Deploy workflow triggered!"
